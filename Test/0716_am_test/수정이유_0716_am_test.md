# 0716 오전 테스트(AM) 수강생 코드 수정 사유서

본 문서는 `D:\DMDG_UT\Test\0716_am_test` 폴더에 포함된 20개 수강생 소스 코드 파일에 대한 분석 결과와 수정한 내용 및 그 사유를 정리한 보고서입니다.  
수정본은 각 파일 이름 뒤에 `_edit`를 추가하여 원본 코드와 구분되도록 생성했습니다.

---

## 1. 개별 수강생 코드 오류 분석 및 수정 내역

### 1.1 `bc_card cbywjc123.py` -> `bc_card cbywjc123_edit.py`
* **기존 문제점**:
  1. **절대 경로 오류**: `C:\Users\최우진\Downloads\bc_card_out2020_03.txt` 절대 경로로 데이터 로딩을 시도하여 타 PC 환경에서 실행 불가.
  2. **Pandas groupby 오류**: `groupby(df['TP_BUZ_NM'])` 형태로 그룹화 함수에 시리즈 데이터를 잘못 인자로 넘겨 `ValueError` 유발.
  3. **구문(Syntax) 오류**: f-string 내부에 문자열 `'편 의 점'`에 단일 따옴표를 사용하여 외부의 단일 따옴표와 충돌해 구문 분석 에러 발생.
* **수정 내역 및 사유**:
  - 데이터 로드 경로를 유연한 상대 경로(`../data/bc_card_out2020_03.txt`)로 수정했습니다.
  - groupby 메서드가 DataFrame 내의 컬럼명인 `'TP_BUZ_NM'` 문자열을 받도록 올바르게 수정했습니다.
  - f-string 내부의 인덱스 검색 부 따옴표를 이중 따옴표(`"편 의 점"`)로 수정하여 문법 오류를 해결했습니다.

### 1.2 `bc_card_amok0323.ipynb` -> `bc_card_amok0323_edit.ipynb`
* **기존 문제점**: 
  - 구글 코랩 전용 임시 경로인 `/content/bc_card_out2020_03.txt` 및 `/content/bc_card.txt`로 지정되어 있어 로컬 주피터 환경에서 `FileNotFoundError` 발생.
* **수정 내역 및 사유**:
  - 파일 조회 경로를 로컬 테스트용 공통 데이터 경로인 `../data/`로 상대 경로 일괄 변경하여 독립적인 구동을 가능하게 하였습니다.

### 1.3 `bc_card_austin0828.zip` -> `bc_card_austin0828_edit.zip`
*(압축 해제된 개별 파이썬 스크립트 수정 폴더 `bc_card_austin0828_edit` 동시 배포)*
* **기존 문제점**: 
  - `01_intro.py` 및 `02_preprocess.py` 파일 내에서 실행 경로 기준으로 상위 디렉토리의 데이터 파일을 읽으려 시도했으며, 실제 폴더 내에 존재하지 않는 `bc_card_output (1).txt` 파일명을 입력해 두어 입출력 장애가 있었습니다.
* **수정 내역 및 사유**:
  - 실행 절대 경로 추출 기준에 공통 데이터셋 폴더(`../data/`) 위치를 추가 적용하였으며, 파일명을 실제 존재하는 이름인 `bc_card_output.txt`로 바로잡았습니다. 수정 후 `bc_card_austin0828_edit.zip` 파일로 재압축하여 저장했습니다.

### 1.4 `bc_card_ddss0703.py` -> `bc_card_ddss0703_edit.py`
* **기존 문제점**: 
  - 로컬 절대 경로 `"C:/Pycharm/0716 analysis/bc_card_out2020_03.txt"`를 하드코딩하여 타 운영 환경에서 실행 불가 에러가 발생했습니다.
* **수정 내역 및 사유**:
  - 해당 절대 경로 부분을 상대 경로 `../data/bc_card_out2020_03.txt`로 대체하여 환경 독립성을 확보했습니다.

### 1.5 `bc_card_dongin3957.py` -> `bc_card_dongin3957_edit.py`
* **기존 문제점**:
  1. **잘못된 파일 참조**: `read_csv()` 함수 구현 시 파일 읽기 대상 경로가 `./data/bc_card.txt`로 고정되어 있어, 전처리 및 분석 타겟 데이터프레임인 `df_out`, `df_output`, `df_card` 세 변수에 각기 다른 세 개의 텍스트 파일이 아닌 한 개의 파일 데이터만 3회 중복 복사하는 논리 오류.
  2. **글로벌 변수 참조 실수**: `analysis` 함수 내에서 로컬 변수인 `df`가 아닌 외부의 전역 데이터프레임 `df_card`를 하드코딩하여 사용해 집계 결괏값 왜곡 우려.
  3. **출력 결여**: 분석 로직은 수행하나 결과를 출력(`print`)하는 코드가 누락되어 결과를 확인할 수 없음.
* **수정 내역 및 사유**:
  - `read_csv` 함수가 읽어올 파일의 경로를 매개변수(`file_path`)로 전달받아 구동되도록 유연화한 뒤, 각 변수 할당 시 정확한 데이터 소스 경로를 넘겨주었습니다.
  - `analysis` 내부의 `df_card` 참조 오류를 입력 매개변수 `df`로 바꾸었고, 분석 결과를 터미널 상에서 확인할 수 있도록 핵심 지표 출력용 print 구문을 배치했습니다.

### 1.6 `bc_card_gtradernhj.ipynb` -> `bc_card_gtradernhj_edit.ipynb`
* **기존 문제점**:
  1. 구글 코랩 절대 경로(`/content/...`) 사용으로 로컬 로드 실패.
  2. 첫 번째 코드 셀 내부의 삼중 따옴표 주석(`'''`)이 마지막 라인에서 닫히지 않아 파일 전체가 정상 작동하지 못하고 `SyntaxError` 발생.
* **수정 내역 및 사유**:
  - 데이터 로드 주소를 `../data/bc_card_out2020_03.txt`로 경로 수정을 완료했으며, 첫 셀 주석의 닫는 부분(`'''`)을 소스 코드 최하단에 보강하여 파싱 구문 오류를 해소했습니다.

### 1.7 `bc_card_h0tt30k.ipynb` -> `bc_card_h0tt30k_edit.ipynb`
* **기존 문제점**: 
  - 데이터 파일 경로가 구글 코랩 가상 경로 `/content/bc_card_out2020_03.txt`로 고정되어 미조회 에러.
* **수정 내역 및 사유**:
  - 상대 경로 `../data/bc_card_out2020_03.txt`로 재정의하여 로컬 구동 문제를 극복했습니다.

### 1.8 `bc_card_heayoon0623.py` -> `bc_card_heayoon0623_edit.py`
* **기존 문제점**: 
  - 구글 코랩 기본 드라이브 경로 `/content/bc_card_out2020_03.txt` 하드코딩 에러.
* **수정 내역 및 사유**:
  - 로컬 테스트에 유효하도록 공통 데이터셋 폴더 기준의 상대 경로 `../data/bc_card_out2020_03.txt`로 변환했습니다.

### 1.9 `bc_card_hikary2007.py` -> `bc_card_hikary2007_edit.py`
* **기존 문제점**:
  1. **잘못된 컬럼명 리네임 매핑 (스와프 오류)**: 판다스 Rename을 수행할 때, 광역시도명인 `MEGA_CTY_NM`을 `시군구명`으로 매핑하고 시군구명인 `CTY_RGN_NM`을 `시도명`으로 잘못 짝지어, 이후 강남구(시군구) 소비 데이터를 집계할 때 값이 일치하지 않아 강남구 편의점 매출이 0원으로 집계되는 치명적인 논리 결함 발생.
  2. **f-string 구문 에러**: f-string 출력문 내 내부 속성값 호출 시 단일 따옴표 내부 단일 따옴표 사용으로 컴파일 구문 에러 유발.
  3. **경로 문제**: `./bc_card.txt` 경로 설정 문제.
* **수정 내역 및 사유**:
  - Rename 딕셔너리의 컬럼 매핑 대상을 정확한 개념으로 정정(시도명 <-> 시군구명)하여 강남구 소비액 집계 로직이 온전하게 동작하도록 했습니다.
  - f-string 내부 따옴표 충돌을 해소(`"이용 금액"`으로 변환)하고 로컬 경로도 `../data/bc_card.txt`로 수정했습니다.

### 1.10 `bc_card_jihye6632.py` -> `bc_card_jihye6632_edit.py`
* **기존 문제점**:
  1. **로컬 하드코딩 절대 경로**: 학생의 개발 디렉토리 `C:/Users/PC/PycharmProjects/.../data/bc_card.txt`가 명기되어 있어 외부 실행 시 즉시 크래시 발생.
  2. **f-string 내부 따옴표 에러**: f-string 따옴표 내부에 다시 double quote가 겹치게 사용되어 구문 파싱 문제 유발.
* **수정 내역 및 사유**:
  - 로컬에서 범용적으로 작동할 수 있게 공통 상대 경로 `../data/bc_card.txt`로 전면 교환하고, f-string 내의 중복 따옴표를 단일 따옴표(`'편 의 점'`)로 감싸며 구문 구성을 다듬었습니다.

### 1.11 `bc_card_middle1106_nave_com_py.py` -> `bc_card_middle1106_nave_com_py_edit.py`
* **기존 문제점**:
  - **분석 조건문 불일치(로직 스킵)**: 코드 내부에서 `if '업종' in df_out.columns` 등의 필터링 절차를 통해 분석 로직을 수행하도록 설계했으나, 원본 텍스트 데이터의 열 헤더는 영문명(`TP_BUZ_NM`, `AMT`, `SEX_CTGO_CD`, `AGE_VAL`, `REG_YYMM`)이기 때문에 조건이 매번 `False`로 판정되어 중요 지표 연산 영역 전체가 아무 결과물도 없이 스킵되는 현상 발생. 
  - `bc_card.txt` 등 로컬 파일 경로 미조회 현상.
* **수정 내역 및 사유**:
  - 데이터 로드에 적합하도록 로컬 상대 경로 `../data/`를 부여하였습니다.
  - 데이터 로딩 직후 영문 컬럼들을 학생이 조건으로 명시한 한글 명칭(`업종`, `이용금액`, `성별`, `연령대`, `이용일자`)으로 치환(rename)하는 전처리 코드를 삽입하여 필터 조건이 충족되면서 실제 총 소비액, 성별 소비 및 분석 레포트 출력이 동작하도록 바로잡았습니다.

### 1.12 `bc_card_pj002321.py` -> `bc_card_pj002321_edit.py`
* **기존 문제점**: 
  - 로컬 실행 시 파일 로드가 누락될 위험이 있는 하드코딩 방식 사용.
* **수정 내역 및 사유**:
  - 데이터셋 폴더인 `../data/` 상대경로를 파일명 앞에 일괄 추가했습니다.

### 1.13 `bc_card_psmsurim22.ipynb` -> `bc_card_psmsurim22_edit.ipynb`
* **기존 문제점**: 
  - 구글 코랩 임시 디렉토리 기반 경로 `/content/bc_card_out2020_03.txt` 하드코딩.
* **수정 내역 및 사유**:
  - 파일 로드 대상 정보를 `../data/bc_card_out2020_03.txt`로 조정하여 파일 미조회 문제를 차단했습니다.

### 1.14 `bc_card_rlawhdgns0619.ipynb` -> `bc_card_rlawhdgns0619_edit.ipynb`
* **기존 문제점**: 
  - 파일 참조가 `bc_card_out2020_03.txt`로 지정되어 있어 실행 위치에 따라 로드 실패 발생.
* **수정 내역 및 사유**:
  - 범용 상대 경로인 `../data/bc_card_out2020_03.txt`로 업데이트를 시행했습니다.

### 1.15 `bc_card_roal1219.py` -> `bc_card_roal1219_edit.py`
* **기존 문제점**: 
  - `bc_card_out2020_03.txt` 파일명 호출 주소 오류 우려.
* **수정 내역 및 사유**:
  - `../data/bc_card_out2020_03.txt` 형태로 상대화하여 데이터 입력을 안정적으로 처리되도록 개선했습니다.

### 1.16 `bc_card_sherlockkang.py` -> `bc_card_sherlockkang_edit.py`
* **기존 문제점**:
  1. **데이터 상대 경로 미일치**: 데이터 폴더(`./data/...`)가 스크립트 실행 위치 기준으로 불일치.
  2. **"거주지 소재 편의점 소비액" 조건 산출 로직 에러**: 문제에서 정의된 거주지 소재는 '구/군(시군구)' 단위로 일치 여부를 매칭해야 하지만, 코드 내에서 광역시도 단위의 코드(`MEGA_CTY_NO`와 `CSTMR_MEGA_CTY_NO`)를 비교하도록 잘못 프로그래밍되어 비정상 집계 데이터를 생성함.
* **수정 내역 및 사유**:
  - 로드 경로를 한 단계 상위인 `../data/bc_card_out2020_03.txt`로 명시해주었습니다.
  - 정확한 시군구 매칭을 보장하기 위해 기존의 메트로급 코드 비교를 구 단위 코드 비교 식(`CTY_RGN_NO == CSTMR_CTY_RGN_NO`)으로 완벽히 치환 및 정정했습니다.

### 1.17 `bc_card_shoh0925.py` -> `bc_card_shoh0925_edit.py`
* **기존 문제점**:
  1. **경로 및 오타 오류**: `/content/bc_card_out_2020_03.txt`로 작성하여 경로 및 파일 이름 중간의 언더스코어(_) 오타로 파일 조회 불가.
  2. **성별 속성타입 체크 실패**: Pandas 수치 자동 추론으로 인해 `SEX_CTGO_CD`가 실수형(1.0, 2.0)으로 데이터 타입이 지정되었으나, 코드 내 조건 매칭문에서는 문자형 `'1.0'`, `'2.0'` 등으로 단독 비교하도록 작성하여 성별 집계 시 일치 항목을 찾지 못하고 전부 `기타/알수없음`으로 결과가 빠지는 버그 발생.
* **수정 내역 및 사유**:
  - 로드 타겟 파일 주소를 `../data/bc_card_out2020_03.txt`로 바로잡았으며, 성별 분기문에서 `str(sex)` 변환 처리를 추가하여 타입 차이로 인한 미매칭 필터 버그를 완벽히 해결했습니다.

### 1.18 `bc_card_shp9826.py` -> `bc_card_shp9826_edit.py`
* **기존 문제점**:
  - **수동 파싱(개행 복구)의 데이터 유실**: `func1()`에서 줄바꿈이 깨진 `bc_card_output.txt`를 수동 토큰 분할하여 데이터프레임으로 복원하는 로직을 수행할 때, 첫 번째 레코드의 데이터를 `datas` 리스트에 담는 과정이 빠져 데이터프레임의 첫 번째 행이 누락(유실)된 채로 2행부터 구성되는 결함이 확인되었습니다.
* **수정 내역 및 사유**:
  - 파일 로드 디렉토리를 `../data/`로 매핑하고, 첫 번째 로우(Row)가 분실되지 않고 DataFrame 내부로 유입될 수 있도록 토큰 추출 로직 및 헤더 분기 처리를 전면 보완했습니다.

### 1.19 `bc_card_tjgus320.ipynb` -> `bc_card_tjgus320_edit.ipynb`
* **기존 문제점**: 
  - 코랩 가상 디렉토리 파일 경로 `/content/bc_card_out2020_03.txt` 하드코딩.
* **수정 내역 및 사유**:
  - 공통 로컬 테스팅 경로인 `../data/bc_card_out2020_03.txt`로 지정 경로를 수정했습니다.

### 1.20 `bc_card_won270048.py` -> `bc_card_won270048_edit.py`
* **기존 문제점**: 
  - 개발자의 PC 로컬 홈 디렉토리를 나타내는 절대 경로인 `C:\Users\enjoy\PycharmProjects\...` 주소를 직접 명시하여 컴파일 장애 유발.
* **수정 내역 및 사유**:
  - 모든 로드 주소 문자열을 환경 비의존적인 상대 경로인 `../data/` 하위 파일로 일괄 정리했습니다.
