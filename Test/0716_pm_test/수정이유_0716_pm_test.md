# 0716 오후 테스트(PM) 수강생 코드 수정 사유서

본 문서는 `D:\DMDG_UT\Test\0716_pm_test` 폴더에 포함된 19개 수강생 소스 코드 파일에 대한 분석 결과와 수정한 내용 및 그 사유를 정리한 보고서입니다.  
수정본은 각 파일 이름 뒤에 `_edit`를 추가하여 원본 코드와 구분되도록 생성했습니다.

---

## 1. 개별 수강생 코드 오류 분석 및 수정 내역

### 1.1 `ml_amok0323.ipynb` -> `ml_amok0323_edit.ipynb`
* **기존 문제점**: 
  - 원격 저장소(`https://raw.githubusercontent.com/...`)에서 타이타닉 CSV 데이터를 로딩할 때 로컬 PC 환경의 SSL 인증서 서명 검증 실패로 인해 `URLError: [SSL: CERTIFICATE_VERIFY_FAILED]` 크래시가 발생하는 문제.
* **수정 내역 및 사유**:
  - 첫 번째 실행 셀 상단에 SSL 인증 검증을 전역적으로 우회 설정해 주는 라이브러리 로드 코드를 선언하여 외부 웹 자원 수신 기능을 원활하게 복구했습니다.

### 1.2 `ml_austin0828.py` -> `ml_austin0828_edit.py`
* **기존 문제점**: 
  - SSL 인증 장벽에 막혀 Github 상의 Titanic CSV를 내려받지 못해 원격 데이터셋 로딩 에러 발생.
* **수정 내역 및 사유**:
  - 스크립트 최상단에 SSL 인증 생략 설정을 강제 적용하여 데이터 연결 오류를 해소했습니다.

### 1.3 `ml_cbywjc123.py` -> `ml_cbywjc123_edit.py`
* **기존 문제점**: 
  - 웹 리소스 로드 시의 보안 프로토콜 검증 통과 실패로 웹 데이터를 데이터프레임으로 변환하지 못하는 현상.
* **수정 내역 및 사유**:
  - SSL 무시 구문을 추가하여 원격 파일 읽기가 즉시 가능하도록 패치했습니다.

### 1.4 `ml_ddss0703.py` -> `ml_ddss0703_edit.py`
* **기존 문제점**: 
  - 보안 소켓(SSL) 통신 요구 과정에서 발생하는 인증 오류로 실행 지연 또는 즉시 차단.
* **수정 내역 및 사유**:
  - SSL 전처리 전역 컨텍스트를 스크립트 초입부에 정의하여 웹 요청 차단을 풀었습니다.

### 1.5 `ml_gtradernhj.ipynb` -> `ml_gtradernhj_edit.ipynb`
* **기존 문제점**:
  1. **SSL 검증 에러**: 웹 데이터 수신 불가.
  2. **구문(Syntax) 오류**: Jupyter Notebook 내 16번째 코드 셀에 작성된 안내 글이 주석 처리되지 않은 상태로 입력되어 코드로 오인해 구문 오류 발생.
* **수정 내역 및 사유**:
  - 첫 번째 셀에 SSL 우회 명령을 주입하고, 문제가 된 16번째 셀 내 일반 설명 텍스트 행들의 앞에 파이썬 주석 기호 `#`를 명시하여 구문 오류를 즉각 해소했습니다.

### 1.6 `ml_h0tt30k.ipynb` -> `ml_h0tt30k_edit.ipynb`
* **기존 문제점**: 
  - SSL 인증 환경 미비에 의한 Titanic 원격 CSV 로딩 실패.
* **수정 내역 및 사유**:
  - 첫 번째 셀에 SSL 검증 패스 처리 로직을 배치하여 원격 주소를 통해 정상 로딩되도록 개선했습니다.

### 1.7 `ml_heayoon0623.ipynb` -> `ml_heayoon0623_edit.ipynb`
* **기존 문제점**: 
  - SSL 서명 확인 과정의 거부로 웹 데이터 원본 확보 실패.
* **수정 내역 및 사유**:
  - 전역 SSL 기본 컨텍스트 설정 해제 로직을 포함하여 정상 실행되도록 수정했습니다.

### 1.8 `ml_hikary2007.py` -> `ml_hikary2007_edit.py`
* **기존 문제점**: 
  - 원격 웹 파일 로드 시 SSL 서명 예외 발생으로 데이터 로딩이 가로막힘.
* **수정 내역 및 사유**:
  - 스크립트 첫 라인 부근에 SSL 기본 필터 미검증용 선언문을 주입하여 원격 파일을 원활하게 불러오도록 수정했습니다.

### 1.9 `ml_jihye6632.py` -> `ml_jihye6632_edit.py`
* **기존 문제점**:
  1. **절대 경로 오류**: `C:/Users/PC/PycharmProjects/.../data/titanic.csv`로 지정되어 타 기기에서 구동 불가.
  2. **SSL 미설정**: URL 연결 시 SSL 검증 에러 발생.
* **수정 내역 및 사유**:
  - 하드코딩된 로컬 PC 전용 주소를 전원의 공통 원격 주소(`https://raw.githubusercontent.com/.../titanic.csv`)로 바꾸었으며, 이 과정에서 발생할 수 있는 SSL 인증 에러를 회피하도록 SSL 무시 옵션을 상단에 얹었습니다.

### 1.10 `ml_middle1106_naver_com.py.py` -> `ml_middle1106_naver_com_edit.py`
* **기존 문제점**:
  1. **가상 데이터셋 대체로 인한 분석 상실**: 실제 Titanic 데이터를 외부에서 호출하지 못하는 한계로, 단 10줄 남짓의 임의 데이터를 만들어 데이터프레임으로 변형하고 모델링을 수행하여 머신러닝 과제 조건의 취지에 어긋남.
  2. **컴파일 불가 구문 포함**: 파이썬 인터프리터 단독 기동 스크립트 파일(`.py`) 내에 주피터 노트북 명령어 `!pip install ...`을 주입하여 `SyntaxError` 발생.
* **수정 내역 및 사유**:
  - 패키지 인스톨 명령 줄(`!pip install`)을 주석으로 바꾸었습니다.
  - 가상 더미 데이터 셋 구문을 제거(주석 처리)하고, 학생 코드에서 요구한 피처 세트(`sex`, `pclass`, `fare`, `age`, `title`) 구성에 맞추어 실제 Titanic 원본 파일을 다운로드받아 전처리하고, 이를 활용하여 결정 트리 의사결정 모델을 학습 및 시각화하도록 코드를 대대적으로 리팩토링했습니다.

### 1.11 `ml_pj002321.py` -> `ml_pj002321_edit.py`
* **기존 문제점**: 
  - 타이타닉 파일 파싱 단계에서 발생하는 SSL 자격 검증 거부 에러.
* **수정 내역 및 사유**:
  - 라이브러리를 통해 SSL 기본 컨텍스트 속성을 해제해 주는 구문을 파일 첫머리에 구성했습니다.

### 1.12 `ml_psmsurim22.ipynb` -> `ml_psmsurim22_edit.ipynb`
* **기존 문제점**:
  1. **Jupyter 매직 구문 에러**: 첫 번째 셀 내부 구글 코랩 매직 명령어 `%tensorflow_version 2.x`가 포함된 try-except 블록이 코랩 외 일반 주피터 엔진에서 `IndentationError` 또는 `SyntaxError`를 유발.
  2. **SSL 미반영**: 데이터 로딩 중단.
* **수정 내역 및 사유**:
  - 셀 내부의 코랩 전용 명령어 기입 줄을 주석화(`pass # %tensorflow_version 2.x`)하고, 데이터 호출에 선행하여 SSL 검증 제거 루틴을 삽입해 문제를 동시에 극복했습니다.

### 1.13 `ml_rlawhdgns0619.py` -> `ml_rlawhdgns0619_edit.py`
* **기존 문제점**: 
  - URL 데이터셋 로드에 수반되는 SSL 서명 예외 에러.
* **수정 내역 및 사유**:
  - 파일 선두에 SSL 예외 검증 무시 구문을 보강 적용했습니다.

### 1.14 `ml_second_cbywjc123.py` -> `ml_second_cbywjc123_edit.py`
* **기존 문제점**: 
  - 웹 로딩 시 통신 인증서 오류로 분석 동작 정지.
* **수정 내역 및 사유**:
  - 최상단부에 SSL 무효 설정 처리를 추가하여 정상 동작을 유도했습니다.

### 1.15 `ml_sherlockkang.py` -> `ml_sherlockkang_edit.py`
* **기존 문제점**: 
  - SSL 라이브러리의 보안 서명 통과 실패 현상.
* **수정 내역 및 사유**:
  - SSL 기본 검증 우회 옵션을 강제 호출하도록 수정했습니다.

### 1.16 `ml_shoh0925.py` -> `ml_shoh0925_edit.py`
* **기존 문제점**:
  1. **파이썬 스크립트 실행 불가**: 파일 첫 행에 주피터 노트북 용 설치 명령인 `!pip install scikit-learn pandas numpy`가 작성되어 스크립트 직접 기동 불가(`SyntaxError` 발생).
  2. **SSL 장벽**: Titanic 데이터 로드 실패.
* **수정 내역 및 사유**:
  - 첫 행의 `!pip` 설치용 구문을 안전하게 주석 처리하고, 상위 라인에 SSL 인증 예외 제외 구문을 추가 적용하여 컴파일 및 실데이터 모델링이 한 번에 진행되도록 정돈했습니다.

### 1.17 `ml_shp9826.py` -> `ml_shp9826_edit.py`
* **기존 문제점**: 
  - SSL 인증 통과가 제한되어 판다스의 웹 읽기 차단 발생.
* **수정 내역 및 사유**:
  - 전역 SSL 인증 수준을 낮춰 웹 CSV 데이터를 안정적으로 수집하게 수정했습니다.

### 1.18 `ml_tjgus320.ipynb` -> `ml_tjgus320_edit.ipynb`
* **기존 문제점**: 
  - SSL 라이브러리의 인증 검사로 타이타닉 CSV 다운로드 불가.
* **수정 내역 및 사유**:
  - 노트북 첫 번째 셀에 SSL 검증 무력화 처리를 진행하여 정상 실행이 가능하도록 수정했습니다.

### 1.19 `ml_won270048.py` -> `ml_won270048_edit.py`
* **기존 문제점**:
  1. **하드코딩 로컬 절대 경로**: 학생의 로컬 PC 경로인 `C:\Users\enjoy\PycharmProjects\PythonProject\0714\titanic.txt`를 활용해 타 환경에서 구동 불가능.
  2. **SSL 미설정**: URL 변경 후 데이터 파싱 에러 발생 가능성.
* **수정 내역 및 사유**:
  - 로컬 절대 디렉토리를 원본 데이터 저장소인 GitHub Titanic CSV URL(`https://raw.githubusercontent.com/.../titanic.csv`)로 완벽 수정하고, 이를 위해 상단부에 SSL 보안 인증 우회 선언 코드를 추가했습니다.
