# 작업지시 007: 최종 [[대화록/006_캐릭터_일관성_피드백_반영_및_대기_계획|캐릭터 일관성 대화록]] 이미지 재생성 및 렌더링 완료

## 1. 사장님(USER)의 요청 사항
* **재시작 지시**: 구글 쿼터 복구 시점에 맞추어 이미지 재생성 및 비디오 렌더링을 다시 완벽하게 진행할 것을 지시함.
* **캐릭터 일관성 및 시나리오 집중**: 더피의 모습을 원본 에셋과 일치하게 집중해서 뽑되, 개별 컷뿐만 아니라 전체 시나리오의 흐름에 녹아드는 연출이 되도록 지시함.

## 2. 데미스(AI)의 조치 내용
* **공식 이미지 생성기로 재생성 (17~25컷 및 30컷)**:
  * 구글 쿼터 복구 확인 후, 공식 이미지 생성기(`generate_image`)를 가동함.
  * `ImagePaths` 매개변수에 `D:\DMDG_UT\YOUTUBE\characters\더피.png`를 지정하여 더피의 외형(노란 비니, 검은 후드티, 하늘색 털 하태 몬스터) 일관성을 정확하게 고정함.
  * 30컷 단체 샷의 경우 `더피.png`, `서씨.png`, `강하루.jpg` 총 3개의 캐릭터 원본 에셋을 동시 매핑하여 공식 오리지널 캐릭터들이 모두 올바르게 씬에 등장하도록 조치함.
* **배경 후처리 자막 및 로고 오버레이**:
  * `fix_29_30.py`를 실행하여 29컷 중앙에 선명하게 `당목담글_로고.png`를 오버레이 합성함.
  * 30컷 하단에 아웃트로 한글 자막("지금, 당신의 목소리로 다음글을 읽어 주세요")을 가독성 높게 오버레이 합성함.
* **최종 더빙 영상 출력 및 파일명 구분**:
  * `YOUTUBE\render_video_30cuts.py`를 실행하여 최종 병합된 한국어(KOR), 영어(ENG), 일본어(JPN) 비디오 3종을 생성함.
  * 기존 프리뷰 파일을 덮어쓰지 않도록 `longform_30cuts_{lang}_final.mp4`로 다국어 영상 파일명을 최종 분리 출력함.
* **Git 히스토리 정리 및 원격 저장소 푸시 완료**:
  * 기존 커밋 히스토리에 포함되었던 100MB 초과 대형 파일(`DMDG_UT_Backup_Final.zip`)을 Git 기록에서 안전하게 배제하고 `.gitignore`에 추가함.
  * 모든 코드 수정사항, 다국어 렌더러 스크립트, 수정된 최종 컷 이미지들, 대화록 폴더를 `git add .` 및 `commit` 하여 원격 저장소(`main` 브랜치)로 성공적으로 푸시 완료함.
