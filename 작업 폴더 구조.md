# 📂 당목담글 (DMDG_UT) 프로젝트 작업 폴더 구조 가이드

이 문서는 **당목담글(DMDG) Great AI 팀**과 개발 파이프라인의 원활한 협업을 위해 전체 프로젝트 폴더 및 파일 구조를 명문화한 가이드입니다.

---

## 📌 전체 디렉토리 요약

| 디렉토리 / 파일 | 구분 | 주요 역할 |
|:---|:---:|:---|
| **`.agent/`** | 폴더 | AI 에이전트들의 페르소나 설정, 포트레이트 사진, 개인 인박스, 텔레그램 봇 소스 |
| **`AI_Knowledge_Base/`** | 폴더 | 중앙 AI 지식 저장소 (회의 관련 로컬 자료 취합 용도) |
| **`RAG/`** | 폴더 | 로컬 문서기반 검색 증강 생성(RAG) 엔진 소스 및 Chroma 벡터 데이터베이스 |
| **`YOUTUBE/`** | 폴더 | 유튜브 콘텐츠 파이프라인 (내부 Projects 폴더들은 서브모듈로 분리 관리) |
| **`museum-react/`** | 서브모듈 | 리액트 기반의 프론트엔드 웹 서비스 소스코드 (독립 저장소 분리) |
| **`museum-pwa/`** / **`sodam-pwa/`** | 서브모듈 | 모바일 친화형 PWA(Progressive Web App) 앱 소스코드 (독립 저장소 분리) |
| **`대화록/`** | 폴더 | 사장님과 AI 간의 핵심 문제 해결 및 피드백 이력 마크다운 문서 |
| **`회의록/`** | 폴더 | 유튜브 콘텐츠 구체화 시나리오, 쇼츠 대본, 작업 실행 계획서 |
| **`IDEA/`** / **`IMPL/`** | 폴더 | 비즈니스 기획 아이디어 및 전략 로드맵 아카이빙 |
| **`sandbox_test/`** | 폴더 | 외부 시스템 영향이 차단된 독립 테스트 샌드박스 폴더 |
| **`reorganize_agents.py`** | 파일 | 에이전트 폴더/파일명 영문 통일용 자동 정리 스크립트 |
| **`telegram_bot.py`** | 파일 | 텔레그램 봇 메인 실행 스크립트 |

---

## 🔍 상세 폴더 구조 및 세부 역할

### 1. 🤖 `.agent/` (AI 에이전트 컨트롤 타워)
AI 에이전트들의 두뇌와 정체성, 소통 채널이 통합되어 있습니다.
*   **`inbox/`**: 사장님이 텔레그램으로 보낸 사진, PDF 등 다양한 파일이 수신자별로 분류되어 저장되는 폴더입니다. (`all`, `demis`, `peggy`, `mustafa`, `jennifer`, `craig`, `haruki`, `zimmer`, `satya`, `sherlock`, `upd`)
*   **`portraits/`**: 텔레그램 봇 응답 시 함께 전송될 각 에이전트들의 고화질 영문 프로필 사진들입니다.
*   **`skills/`**: 에이전트 개별 역할군 지침서(`SKILL.md`) 폴더입니다.
*   **`telegram/`**: 봇 구동 로그(`bot.log`)와 프로필 일괄 전송 스크립트 등이 담겨 있습니다.
*   **`TEAM_PROFILES.md`**: 9명 거장 팀의 단축키, 직책, 주요 스킬을 한눈에 보는 가이드북입니다.

### 2. 🧠 `AI_Knowledge_Base/` & `.agent/knowledge_base/`
에이전트들이 지능적으로 답변하기 위해 참고하는 중앙 지식 허브입니다.
*   **`.obsidian/`**: 문서 간의 연결고리를 시각화하는 지식 그래프 도구 Obsidian 설정입니다.
*   **`meetings/`**: 로컬 모델(supergemma4)의 학습 데이터, 파인튜닝 로드맵, 유튜브 채널별 브랜딩 아이덴티티 규칙 등이 모여 있습니다.

### 3. 🔍 `RAG/` (Retrieval-Augmented Generation)
인터넷 연결이 없는 로컬 오프라인 환경에서도 사장님의 내부 문서에 기반해 정확한 답변을 낼 수 있게 돕는 폴더입니다.
*   **`ingest.py`**: 신규 가이드나 문서를 읽어서 청크(Chunk)로 쪼개고 임베딩하여 데이터베이스에 저장합니다.
*   **`query.py`**: 데이터베이스에서 질문과 가장 유사도가 높은 맥락을 뽑아내어 LLM에 전달합니다.
*   **`chroma_db/`**: 로컬 벡터 스토어(ChromaDB) 데이터 폴더입니다.

### 4. 🎬 `YOUTUBE/` (유튜브 자동화 스튜디오)
유튜브 채널 운영을 위한 렌더링 스크립트 및 에셋을 관리하는 핵심 폴더입니다.
*   **`Projects/`**: `Subway_Shorts`, `Geumdeungjisa` 등 각 유튜브 영상 프로젝트들이 깃허브 서브모듈(Submodule) 형태로 독립적으로 관리되는 폴더입니다.
*   **`scripts/`**: 영상 렌더링, 오디오 병합 등을 담당하는 파이썬 및 FFmpeg 자동화 스크립트가 위치합니다.

### 5. 💻 독립 웹/앱 프로젝트 (서브모듈)
메인 저장소의 무게를 줄이고 배포를 최적화하기 위해, 아래 웹 프로젝트들은 개별 깃허브 저장소(Submodule)로 분리되었습니다.
*   **`museum-react/`**: 당목담글 전시관 체험 등이 구현된 리액트 모던 웹 애플리케이션입니다.
*   **`museum-pwa/`**: 정적 웹 기술 기반의 박물관 PWA 클라이언트입니다.
*   **`sodam-pwa/`**: 소담 서비스를 위한 React + Firebase 기반 PWA 클라이언트입니다.

### 6. 📜 `대화록/` & `회의록/`
팀원들과 사장님 간의 교감, 의사결정의 발자취입니다.
*   **`대화록/`**: 에러 디버깅 내역, 캐릭터 일관성 피드백 조치 등 실제로 겪은 작업 히스토리를 차례대로 아카이빙합니다.
*   **`회의록/`**: 향후 진행할 개발 계획서, 쇼츠 타임라인 가이드라인, 썸네일 기획 등을 포함한 상세 시나리오 모음입니다.

---

## ⚠️ 작업 시 준수 사항 (세뇌 지침)
1. **기존 파일 덮어쓰기 금지 (Overwrite 금지)**: 지식의 일관성 및 로컬 AI의 지식 그래프 확장을 위해 기존 계획이나 대본은 덮어쓰지 않고 고유 버전(`v2`, `v3` 등)이나 고유 번호(예: `011_...`)를 매겨 새로운 파일로 생성합니다.
2. **에이전트 경로 참조 일관성**: 이미지나 프로필을 참조할 때는 반드시 `.agent/portraits/` 폴더 내의 정렬된 파일명을 참조해야 텔레그램 봇과 프론트엔드 연동에 에러가 발생하지 않습니다.
