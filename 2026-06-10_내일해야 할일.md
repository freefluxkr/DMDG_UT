# 📝 내일 해야 할 일 (2026-06-10 작성)

## 1. 텔레그램 봇(`bot.py`) 수신 및 이미지 처리 점검
*   **문제 상황**: 텔레그램에서 `/p` (또는 `/peggy`) 태그와 함께 보낸 사진이 `.agent/inbox/peggy/` 폴더로 정상적으로 수신되지 않는 현상 발생.
*   **조치 목표**:
    *   `봇_시작.bat` 실행 시 봇이 백그라운드에서 정상적으로 메시지를 수신하는지 서버 연결 상태 점검.
    *   사진 수신 시 **자동으로 1/4 크기로 축소(Resize)** 하여 저장 및 출력하는 로직 파이프라인 확인.

## 2. 주요 `.bat` 실행 파일 리스트 및 용도 숙지 (바탕화면 단축키 활용)
프로젝트 내 흩어져 있던 주요 배치 파일 목록입니다. 상황에 맞게 활용합니다.

1. `봇_시작.bat` : 텔레그램 봇 단독 실행용
2. `dmdg_morning_start.bat` : 아침 업무 전체 시스템 구동용
3. `backup_and_shutdown.bat` : 일과 후 작업 백업 및 PC 종료용
4. `force_backup.bat` / `just_backup.bat` : 작업 중간 수동 백업용
5. `init_phase3.bat` : 페이즈 3 초기화용
6. `.agent\telegram\run_bot.bat` : 봇 내부 스크립트 실행
7. `.agent\telegram\run_report.bat` : 봇 내부 리포트 스크립트 실행

## 3. 비 오는 날 대중교통 쇼츠(Subway_Shorts) 후속 작업
*   취합된 텔레그램 DM(유튜브 제목/설명글 아이디어)을 바탕으로 최종 업로드 메타데이터 픽스.
*   완성된 썸네일(V5) 및 스크립트(`rainy_subway_shorts.md`)를 기반으로 영상 에셋 렌더링 파이프라인 가동.
